[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["chmod +x scripts/railway-build.sh", "bash scripts/railway-build.sh"]

[start]
cmd = "pnpm exec next start"

